version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_DOMAIN=biscuit-api-access-service
      - TOKEN_LIFETIME_MINUTES=60
      - DELEGATED_TOKEN_LIFETIME_MINUTES=5
      - HOST=0.0.0.0
      - PORT=8000
    volumes:
      - .:/app
